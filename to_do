
phpmemory_limit=512M #or what ever you want it set to
short_open_tag=on

sed -i 's/memory_limit = .*/memory_limit = '${phpmemory_limit}'/' /etc/php5/apache2/php.ini
sed -i 's/short_open_tag .*/short_open_tag '${short_open_tag}'/' /etc/php5/apache2/php.ini



SAAS

	регистрация
	активация
	поддержка?



написать о новых функциях + облако!

gitlab
support-portal

mediawiki

войти под кем-то?




привязка клиентов к подразделениям.
CSV-выгрузка отчётов







ID заявки (по номеру или случайно)


zip
memory_limit 512M
=================================================================



-доработать поиск (несколько областей поиска и если хоть в одной что-то есть то вывести)

	Поиск по заявкам:
		- в тексте
		- в теме
		- номер

	Поиск по комментариям заявки
		-текст комментария

	Поиск по Центру знания
		-текст документа

	Поиск по пользователями


=================================================================

function send_json() {
	//API Url
$url = 'http://example.com/api/JSON/create';
 
//Initiate cURL.
$ch = curl_init($url);
 
//The JSON data.
$jsonData = array(
    'username' => 'MyUsername',
    'password' => 'MyPassword'
);
 
//Encode the array into JSON.
$jsonDataEncoded = json_encode($jsonData);
 
//Tell cURL that we want to send a POST request.
curl_setopt($ch, CURLOPT_POST, 1);
 
//Attach our encoded JSON string to the POST fields.
curl_setopt($ch, CURLOPT_POSTFIELDS, $jsonDataEncoded);
 
//Set the content type to application/json
curl_setopt($ch, CURLOPT_HTTPHEADER, array('Content-Type: application/json')); 
 
//Execute the request
$result = curl_exec($ch);
}



Обратный API по действиям:

напр действие: создание заявки

	запуск скрипта с параметрами json ()

	-действие
	-кто инициатор
	-время
	-код заявки

=================================================================

API:

	- авторизация

    - получение списка заявок
    	- входящие
    	- исходящие
    	- архив
    	- поиск

    - получение определённое заявки
    	- выполение действия
    		- блок/разблок
    		- выполн/невыполн
    	- комментарий
    		- получить
    		- оставить
    	- перенаправление
    		- получить список отделов
    		- получить список пользователей отдела

    - создание заявки
    	- от кого?
    		- получить список пользователей
    	- кому?
    		- --//--
    	- приоритет
    		список приоритетов
    	- тема
    	- текст
    	- файл

    - список пользователей
    -----------------------------------------------
напр: запрос:
    {
  "mode": "auth",
  "login": "system",
  "pass": "1234",
  "device_token": "1"
}

и такой ответ:
[
  {
    "status" : "ok",
    "uniq_id" : "7371a131b959f3527cbde59f0e5caf96",
    "error_description" : null
  }
]




=================================================================
-фильтрация заявок (вместо поля поиска на странице заявок)
Фильтр заявок:

	- ID
	- Приоритет
	- Тема
	- Пользователь
	- Дата создания
	- Автор
	- Статус

=================================================================
увеличить типы полей:
- дата
- textarea


ДОП ПОЛЯ ПОЛЬЗОВАТЕЛЕЙ


	- в создании заявки? выводить
	- в просмотре заявки инфа о доп полях

	- в списке пользователя
	- в профиле пользователя


или общедостпное поле с данными 
или скрытые поля ssh и тд и и тп

bkb






=================================================================
-дополнительные поля заявок (стоимость работ и тд)
	доп поля самих пользователей?

Тип поля: 
	input_text
	select
	multiselect
		SOURCE=> DB, TABLE, field name



база с параметрами

инфа шаблон:
	ticket_fields
		id
		type
		name

		инфа к заявке:
	ticket_additional
		id
		val
		ticket_id


----------------------------

checkbox(active/no), название поля, плейсхолдер, value /del
+add
сохранить
-----------------------

вывести в форме заявки все доп поля:
название поля, плейсхолдер, value
---------
создать заявку:
	добавить в ticket_data все значения
---------
Вывести заявку:
	показать все доп поля к заявке
	вывести всё с ticket_data где ticket_id=...

ПОЛЕ ЗАЯВКИ:

	ticket_fields
		id
		type
		name
		status

	ticket_data
		ticket_id
		val
		field_id
		name


=================================================================
	идея: в рассмотрении, в разработке, сделано, отклонено, в планах
	проблема: известна, не проблема, в разработке, решена


portal_posts
	id
	title
	type(1,2,3,4)
	author_id
	date
	status(1,2,3,4,5)
	msg





id, name, parent_id(0)
1 	one   0
2 	two   1
3   three 1














-nodejs on proxy_port ( /node ==> 3005 )
-файл в центре знаний
-шаблоны клиентских заявок +

-API !!! 


-labels/tags при создании заявки (база тэгов), поиск по тэгам (опция заявки тэги напр feture, bugs, qa?)

-информирование админа если заявка просрочена


- SMSC-plugin
- раздел файлов

-внешняя база знаний
-внешний портал:
	- Центр знаний
	- Новости
	- Объявления
	- Создание заявки
	- Регистрация
	- roadmap
	- сообщения с тэгами
		use responce
			- предлдожить идея
			- сообщить проблему
			- задать вопрос
			- благодарность
		голосование/лайки

		стадии: решено, проверка, не подтвердилось


заявки объединении их

	статусы: 
	идея: в рассмотрении, в разработке, сделано, отклонено, в планах
	проблема: известна, не проблема, в разработке, решена


- Редактирование заявки (крайний срок)

-qrcode
-pdf

-закрепление пользователей за определенным специалистом


-удаление заявок админом

+apt-get install php5-imap ldap
+ поменять инструкцию по установке




1. Когда создаешь клиента, у него неактивны галочки на добавление и редактирование клиента. Но после того, как зайдешь в редактирование этого пользователя они активны. Это нормально?
2. Если сразу поставить галочку на LDAP, то не дает создать пользорвателя так как поле пароля пустое. Но оно затемнено и недоступно.










server {
  listen       80;
  server_name  localhost;

  location /faye {
    proxy_pass http://localhost:9292;
  }
}



OR
server {
    listen 80;
    root /home/chl/chat;
    index index.html index.htm;
 
    server_name _;
 
    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}




!Если не заполнены кому в приёме из почты?

===========================

какие сообщения кому


ticket_create
ticket_refer
ticket_comment
ticket_lock
ticket_unlock
ticket_ok
ticket_no_ok



доп параметры заявки (крайний срок)

============================

notificaion_mail_type: 0,0,0,0,0,0,0

$CONF_DB_MAIN_NT['ticket_create']=$arr[0];
if $CONF_DB_MAIN_NT['ticket_create'] == 0





help instruction page



если в пользователе ключ- удалён то удалить всю инфу о нём оставив только фио



approved_info
	---
comments
	в комментариях удалённый - может потеряться суть?
	=остаётся
helper
	файл справки остаётся?
	=остаётся
messages
	сообщения и переписка чата?
	=остаётся
news
	удалить новости с ним?
	=остаются
notes
	удалить все заметки?
	=удалить

ticket_log
	очистить лог
	=остаются
tickets
	очистить заявки
	=остаются
users
	пометка как deleted
	при переходе на удалённого пользователя








			
			calendar+todolist
			===================


//client


//удаление заявок
	файлы заявки
	комментарии
	лог

confirmation ? yes ==> 
	ticket_disable
	comments_delete from
	log_delete


//удаление пользователей
		пользователь остаётся но пометка удалён и:
		не участвует нигде больше.

//удаление отделов

